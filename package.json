{
  "name": "deck",
  "devDependencies": {
    "@marp-team/marp-cli": "^0.23.0"
  },
  "scripts": {
    "start": "marp --theme-set ./themes -s --allow-local-files ./slides",
    "build": "marp --theme-set ./themes --allow-local-files --pdf ./slides"
  }
}
